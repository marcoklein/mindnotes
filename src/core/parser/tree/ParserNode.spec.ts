
import 'mocha';
import { ParserTree } from './ParserTree';



describe('Parser Node Test', () => {


	let tree = new ParserTree();

	beforeEach(() => {
		tree = new ParserTree();
	});

	// it('Should create a new node', () => {
	// 	// given when
	// 	let node = new ParserNode(0, tree, );
	// 	// then
	// 	expect(tree.nodesAtLine).to.be.empty;
	// 	expect(tree.nodes).to.be.empty;
	// });
	
});
